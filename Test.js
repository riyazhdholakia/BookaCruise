describe("A suite is just a function", signup() {
    //calls signup function from signup.html and test the function. var a;
    it("and so is a spec", function() { a = true;
    expect(a).toBe(true); });
    });

    describe("A signup is just a function", signup() {
        //calls signup function from signup.html and test the function. var a;
        it("and so is a spec", function() { a = true;
        expect(a).toBe(true); });
        });

        describe("A signup is just a function", signup() {
            //calls signup function from signup.html and test the function. var a;
            it("and so is a spec", function() { a = true;
            expect(a).toBe(true); });
            });

            describe("A login is just a function", signup() {
                //calls login function from login.html and test the function. var a;
                it("and so is a spec", function() { a = true;
                expect(a).toBe(true); });
                });

                describe("A suite is just a function", login() {
                    //calls login function from login.html and test the function to see if the login function is working. var a;
                    it("and so is a spec", function() { a = true;
                    expect(a).toBe(true); });
                    });

                    describe("A suite is just a function", deparature() { var a;
                        it("can use the , function() { expect(this.foo).toEqual(0); this.bar = "Deparature";
describe("A destination is just a function", destination() {
//calls login function from destination.html and test the all functions. var a;
it("and so is a spec", function() { a = true;
expect(a).toBe(true); });
});

describe("A deperature is just a function", deparature() { var a;
    it("can use the , function() {
        expect(this.foo).toEqual(0); this.bar = "Deparature";

        describe("A suite is just a function", roomSel() {
            //calls room function from room.html and test the function to see if the room selection function is working. var a;
             
            it("and so is a spec", function() { a = true;
             
            expect(a).toBe(true);
            });
            });

            
            describe("A suite is just a function", roomSel() {
                //calls room function from room.html and test the function to see if the room selection function is working. var a;
                 
                it("and so is a spec", function() { a = true;
                 
                expect(a).toBe(true);
                });
                });


describe("A suite is just a function", roomSel() {
//calls room function from room.html and test the function to see if the room selection function is working. var a;
 
it("and so is a spec", function() { a = true;
 
expect(a).toBe(true);
});
});

describe("A date is just a function", dateSel() {
    //calls room function from dates.html and test the function to see if the room selection function is working. var a;
     
    it("and so is a spec", function() { a = true;
     
    expect(a).toBe(true);
    });
    });

    describe("A date is just a function", dateSel() {
        //calls room function from dates.html and test the function to see if the room selection function is working. var a;
         
        it("and so is a spec", function() { a = true;
         
        expect(a).toBe(true);
        });
        });

        
        describe("A date is just a function", dateSel() {
            //calls room function from dates.html and test the function to see if the room selection function is working. var a;
             
            it("and so is a spec", function() { a = true;
             
            expect(a).toBe(true);
            });
            });

            
            describe("Database will match the requirements", dataBaseSel() {
                //calls room function from dataBase.html and test the function to see if the database function is working. var a;
                 
                it("and so is a spec", function() { a = true;
                 
                expect(a).toBe(true);
                });
                });

                
                describe("Database will match the requirements", dataBaseSel() {
                    //calls room function from dataBase.html and test the function to see if the database function is working. var a;
                     
                    it("and so is a spec", function() { a = true;
                     
                    expect(a).toBe(true);
                    });
                    });

                    
                    describe("Database will match the requirements", dataBaseSel() {
                        //calls room function from dataBase.html and test the function to see if the database function is working. var a;
                         
                        it("and so is a spec", function() { a = true;
                         
                        expect(a).toBe(true);
                        });
                        });

                        
                        describe("User forgot to enter the security code for his/her credit card", paymentSel() {
                            //calls room function from payment.html and test the function to see if the database function is working. var a;
                             
                            it("and so is a spec", function() { a = true;
                             
                            expect(a).toBe(true);
                            });
                            });

                            
describe("User entered all the required information", paymentSel() {
//calls room function from payment.html and test the function to see if the database function is working. var a;
 
it("and so is a spec", function() { a = true;
 
expect(a).toBe(true);
});
});

describe("User leave one box empty", confirmationSel() {
    //calls room function from confirmation.html and test the function to see if the database function is working. var a;
     
    it("and so is a spec", function() { a = true;
     
    expect(a).toBe(true);
    });
    });

    
    describe("User will get a confirmation", confirmationSel() {
        //calls room function from confirmation.html and test the function to see if the database function is working. var a;
         
        it("and so is a spec", function() { a = true;
         
        expect(a).toBe(true);
        });
        });

        
        describe("User will get successful message from the employee. ", contactSel() {
            //calls room function from contact.html and test the function to see if the database function is working. var a;
             
            it("and so is a spec", function() { a = true;
             
            expect(a).toBe(true);
            });
            });

            
            describe("User will get unsuccessful message from the employee. ", contactSel() {
                //calls room function from contact.html and test the function to see if the database function is working. var a;
                 
                it("and so is a spec", function() { a = true;
                 
                expect(a).toBe(true);
                });
                });

                
                describe(“User is trying to logout from the website and get successful", logoutSel() {
                    //calls room function from logout.html and test the function to see if the database function is working. var a;
                     
                    it("and so is a spec", function() { a = true;
                     
                    expect(a).toBe(true);
                    });
                    });

                    
                    describe(“User is trying to logout from the website and get an error", logoutSel() {
                        //calls room function from logout.html and test the function to see if the database function is working. var a;
                         
                        it("and so is a spec", function() { a = true;
                         
                        expect(a).toBe(true);
                        });
                        });
                        




    